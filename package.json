{
  "name": "azure-devops-workitem-compare",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "electron": "^36.2.0",
    "electron-builder": "^26.0.12",
    "pkg": "^5.8.1"
  },
  "main": "main.js",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "electron": "electron .",
    "build:standalone": "npm run prepkg && npx pkg server/index.js --targets node18-macos-x64,node18-linux-x64,node18-win-x64",
    "prepkg": "cd client && npm install && npm run build && cd .. && rm -rf client_build && cp -R client/build client_build",
    "start": "react-scripts start",
    "build:electron": "electron-builder"
  }
}
